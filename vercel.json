{
  "version": 2,
  "builds": [
    { "src": "server/dist/server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/_api/(.*)", "dest": "server/dist/server.js" },
    { "src": "/(.*)", "dest": "server/dist/server.js" }
  ]
}
