{
  "scripts": {
    "build": "npm run build:frontend && npm run build:server",
    "build:frontend": "cd angular-project && npm run build",
    "start": "npm run build && node server/dist/server.js",
    "build:server": "cd server && npm install && npm run build && npm run copy-public",
    "dev": "npm run build:frontend && ts-node server/server.ts"
  },
  "dependencies": {
    "@vercel/node": "^5.0.2",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2"
  }
}
