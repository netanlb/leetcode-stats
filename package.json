{
  "scripts": {
    "build": "npm run build:frontend && npm run build:server",
    "build:server": "cd server && npm install",
    "build:frontend": "cd angular-project && npm install && npm run build"
  },
  "dependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "@vercel/node": "^5.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
